<!-- AUTO-GENERATED by script/sync-openapi - do not edit by hand -->

# Key schemas

## Server

Required: `uuid`, `label`, `state`, `billing_mode`, `os_template`, `plan`, `specs`, `created_at`.

Fields: `billing_interval`, `billing_mode`, `created_at`, `ip_space`, `label`, `location`, `os_template`, `plan`, `primary_ipv4`, `primary_ipv6`, `provisioning_status`, `specs`, `state`, `uuid`.

## ServerBilling

Required: `billing_mode`, `interval`, `line_items`, `total`.

Fields: `billing_mode`, `est_monthly`, `free`, `interval`, `line_items`, `total`, `uninvoiced_amount`, `uninvoiced_hours`.

## Bandwidth

Required: `range`, `inbound_bytes`, `outbound_bytes`, `total_bytes`.

Fields: `inbound_bytes`, `outbound_bytes`, `range`, `total_bytes`.

## DnsRecord

Required: `id`, `name`, `type`, `content`, `domain`.

Fields: `content`, `domain`, `id`, `name`, `type`.

## Plan

Required: `id`, `code`, `name`, `description`, `specs`, `prices`.

Fields: `code`, `description`, `id`, `name`, `prices`, `specs`.

## OsTemplatesList

Required: `os_templates`.

Fields: `os_templates`.

## Pagination

Required: `page`, `per_page`, `previous_page`, `next_page`, `last_page`, `total_entries`.

Fields: `last_page`, `next_page`, `page`, `per_page`, `previous_page`, `total_entries`.

## ActionQueued

Required: `status`, `action`.

Fields: `action`, `status`.

## Bandwidth

Required: `range`, `inbound_bytes`, `outbound_bytes`, `total_bytes`.

Fields: `inbound_bytes`, `outbound_bytes`, `range`, `total_bytes`.

## BillingLineItem

Required: `label`, `product_code`, `quantity`, `unit_price`, `amount`.

Fields: `amount`, `label`, `product_code`, `quantity`, `unit_price`.

## DnsRecord

Required: `id`, `name`, `type`, `content`, `domain`.

Fields: `content`, `domain`, `id`, `name`, `type`.

## DnsRecordCreateRequest

Required: `dns_record`.

Fields: `dns_record`.

## DnsRecordEnvelope

Required: `dns_record`.

Fields: `dns_record`.

## DnsRecordUpdateRequest

Required: `dns_record`.

Fields: `dns_record`.

## DnsRecordsList

Required: `dns_records`, `meta`.

Fields: `dns_records`, `meta`.

## Error

Required: `error`.

Fields: `error`.

## IsosList

Required: `isos`.

Fields: `isos`.

## Location

Required: `code`, `name`, `display_name`.

Fields: `code`, `country`, `display_name`, `name`.

## LocationsList

Required: `locations`.

Fields: `locations`.

## Meta

Required: `pagination`.

Fields: `pagination`.

## OsTemplateFamily

Required: `title`, `logo`, `series`.

Fields: `logo`, `series`, `title`.

## OsTemplateSeries

Required: `title`, `version`, `code`.

Fields: `code`, `title`, `version`.

## OsTemplatesList

Required: `os_templates`.

Fields: `os_templates`.

## Pagination

Required: `page`, `per_page`, `previous_page`, `next_page`, `last_page`, `total_entries`.

Fields: `last_page`, `next_page`, `page`, `per_page`, `previous_page`, `total_entries`.

## Plan

Required: `id`, `code`, `name`, `description`, `specs`, `prices`.

Fields: `code`, `description`, `id`, `name`, `prices`, `specs`.

## PlanSpec

Required: `name`, `quantity`, `unit`.

Fields: `name`, `quantity`, `unit`.

## PlansList

Required: `plans`.

Fields: `plans`.

## Server

Required: `uuid`, `label`, `state`, `billing_mode`, `os_template`, `plan`, `specs`, `created_at`.

Fields: `billing_interval`, `billing_mode`, `created_at`, `ip_space`, `label`, `location`, `os_template`, `plan`, `primary_ipv4`, `primary_ipv6`, `provisioning_status`, `specs`, `state`, `uuid`.

## ServerBilling

Required: `billing_mode`, `interval`, `line_items`, `total`.

Fields: `billing_mode`, `est_monthly`, `free`, `interval`, `line_items`, `total`, `uninvoiced_amount`, `uninvoiced_hours`.

## ServerCreateRequest

Required: `server`.

Fields: `server`.

## ServersList

Required: `servers`, `meta`.

Fields: `meta`, `servers`.

## SshHostKey

Required: `type`, `fingerprint`.

Fields: `fingerprint`, `type`.

## SshHostKeysList

Required: `ssh_host_keys`.

Fields: `ssh_host_keys`.

## SshKey

Required: `id`, `name`, `key`, `username`, `key_type`, `fingerprint_sha256`, `created_at`.

Fields: `created_at`, `fingerprint_sha256`, `id`, `key`, `key_type`, `name`, `username`.

## SshKeyCreateRequest

Required: `ssh_key`.

Fields: `ssh_key`.

## SshKeyEnvelope

Required: `ssh_key`.

Fields: `ssh_key`.

## SshKeysList

Required: `ssh_keys`.

Fields: `ssh_keys`.

